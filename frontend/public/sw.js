// This is a placeholder service worker using Workbox
// The real service worker will be generated by the Vite PWA plugin

self.addEventListener('install', (event) => {
  self.skipWaiting();
});

self.addEventListener('activate', (event) => {
  event.waitUntil(clients.claim());
});

// This service worker file is a placeholder
// The actual service worker functionality is managed by the Vite PWA plugin
// which will generate a proper service worker during the build process
